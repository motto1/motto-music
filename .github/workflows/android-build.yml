name: Build Android APK

on:
  push:
    branches:
      - main
    tags:
      - 'v*'
  pull_request:
    branches:
      - main
  workflow_dispatch:

permissions:
  contents: write

env:
  GRADLE_VERSION: "8.12"
  GRADLE_DIST_URL: "https://services.gradle.org/distributions/gradle-8.12-all.zip"

jobs:
  build-android:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Java
        uses: actions/setup-java@v4
        with:
          distribution: 'temurin'
          java-version: '17'

      - name: Setup Flutter
        uses: subosito/flutter-action@v2
        with:
          channel: 'stable'
          cache: true

      # 缓存完整的 Gradle 目录（包括 wrapper 和依赖）
      - name: Cache Gradle
        uses: actions/cache@v4
        id: gradle-cache
        with:
          path: |
            ~/.gradle/wrapper
            ~/.gradle/caches
          key: gradle-${{ runner.os }}-${{ hashFiles('android/gradle/wrapper/gradle-wrapper.properties', 'android/**/*.gradle*') }}
          restore-keys: |
            gradle-${{ runner.os }}-

      # 如果缓存未命中，从 GitHub Release 下载 Gradle
      - name: Download Gradle from Release (if not cached)
        if: steps.gradle-cache.outputs.cache-hit != 'true'
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          echo "📥 Cache miss - downloading Gradle from GitHub Release..."
          
          # 使用 gh CLI 下载（支持私有仓库）
          gh release download gradle-deps -p "gradle-8.12-all.zip" -D /tmp
          
          if [ ! -f "/tmp/gradle-8.12-all.zip" ]; then
            echo "❌ Failed to download Gradle from Release"
            exit 1
          fi
          
          mv /tmp/gradle-8.12-all.zip /tmp/gradle.zip
          
          # 验证 zip 文件
          echo "🔍 Verifying downloaded file..."
          if ! unzip -t /tmp/gradle.zip > /dev/null 2>&1; then
            echo "❌ Downloaded file is not a valid zip"
            exit 1
          fi
          
          # 准备 wrapper 目录
          WRAPPER_DIR="$HOME/.gradle/wrapper/dists/gradle-8.12-all"
          CACHE_DIR="$WRAPPER_DIR/cached"
          mkdir -p "$CACHE_DIR"
          
          # 解压到临时目录
          echo "📦 Extracting Gradle..."
          TEMP_DIR=$(mktemp -d)
          unzip -q /tmp/gradle.zip -d "$TEMP_DIR"
          
          # 移动到 wrapper 期望的位置
          mv "$TEMP_DIR"/gradle-*/* "$CACHE_DIR/" 2>/dev/null || mv "$TEMP_DIR"/gradle-* "$CACHE_DIR/gradle-8.12"
          
          # 复制 zip 文件并创建 .ok 标记（wrapper 需要）
          cp /tmp/gradle.zip "$CACHE_DIR/gradle-8.12-all.zip"
          touch "$CACHE_DIR/gradle-8.12-all.zip.ok"
          
          # 清理临时文件
          rm -rf "$TEMP_DIR" /tmp/gradle.zip
          
          echo "✅ Gradle installed to: $CACHE_DIR"
          ls -lh "$CACHE_DIR" | head -10


      # 配置 Gradle wrapper 使用缓存的版本
      - name: Configure Gradle wrapper
        run: |
          WRAPPER_DIR="$HOME/.gradle/wrapper/dists/gradle-8.12-all"

          # 检查是否有缓存的 Gradle
          if [ -d "$WRAPPER_DIR" ]; then
            # 找到实际的 Gradle 目录
            GRADLE_DIR=$(find "$WRAPPER_DIR" -maxdepth 2 -name "gradle-8.12" -type d 2>/dev/null | head -1)
            if [ -z "$GRADLE_DIR" ]; then
              GRADLE_DIR=$(find "$WRAPPER_DIR" -maxdepth 2 -type d -name "bin" 2>/dev/null | head -1 | xargs dirname)
            fi

            if [ -n "$GRADLE_DIR" ] && [ -d "$GRADLE_DIR" ]; then
              echo "Found Gradle at: $GRADLE_DIR"
              echo "GRADLE_HOME=$GRADLE_DIR" >> $GITHUB_ENV
            fi
          fi

          # 显示 wrapper 配置
          cat android/gradle/wrapper/gradle-wrapper.properties

      - name: Get dependencies
        run: flutter pub get

      # 初始化 Gradle Wrapper
      - name: Initialize Gradle Wrapper
        working-directory: android
        run: |
          chmod +x ./gradlew
          
          echo "🚀 Initializing Gradle wrapper..."
          if ./gradlew --version --no-daemon; then
            echo "✅ Gradle wrapper initialized successfully"
          else
            echo "❌ Gradle initialization failed"
            exit 1
          fi

      - name: Build APK
        run: flutter build apk --split-per-abi --release
        env:
          GRADLE_OPTS: "-Dorg.gradle.jvmargs=-Xmx4096m"

      - name: Build App Bundle
        run: flutter build appbundle --release

      - name: Get version
        id: version
        run: |
          VERSION=$(grep '^version:' pubspec.yaml | awk '{print $2}' | cut -d+ -f1)
          echo "version=$VERSION" >> $GITHUB_OUTPUT
          echo "Version: $VERSION"

      - name: Rename APKs
        run: |
          VERSION=${{ steps.version.outputs.version }}
          mkdir -p artifacts

          cp build/app/outputs/flutter-apk/app-armeabi-v7a-release.apk \
             artifacts/MottoMusic-${VERSION}-armeabi-v7a.apk

          cp build/app/outputs/flutter-apk/app-arm64-v8a-release.apk \
             artifacts/MottoMusic-${VERSION}-arm64-v8a.apk

          cp build/app/outputs/flutter-apk/app-x86_64-release.apk \
             artifacts/MottoMusic-${VERSION}-x86_64.apk

          cp build/app/outputs/bundle/release/app-release.aab \
             artifacts/MottoMusic-${VERSION}.aab

      - name: Upload APK Artifacts
        uses: actions/upload-artifact@v4
        with:
          name: android-apk-${{ steps.version.outputs.version }}
          path: artifacts/*.apk
          retention-days: 30

      - name: Upload AAB Artifact
        uses: actions/upload-artifact@v4
        with:
          name: android-aab-${{ steps.version.outputs.version }}
          path: artifacts/*.aab
          retention-days: 30

      - name: Create Release
        if: startsWith(github.ref, 'refs/tags/v')
        uses: softprops/action-gh-release@v1
        with:
          files: |
            artifacts/*.apk
            artifacts/*.aab
          body: |
            ## Motto Music ${{ steps.version.outputs.version }}

            ### 📦 Android 安装包

            - **arm64-v8a**: 适用于大多数现代 Android 设备 (推荐)
            - **armeabi-v7a**: 适用于较旧的 32 位 Android 设备
            - **x86_64**: 适用于 x86 架构设备（模拟器等）
            - **AAB**: Google Play Store 发布包
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
